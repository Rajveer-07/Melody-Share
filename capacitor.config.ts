
import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.lovable.enhance.your.home',
  appName: 'enhance-your-home',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
